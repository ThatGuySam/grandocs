{
	"name": "grandocs-mcp",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"description": "Stateless Cloudflare Worker MCP server: docs search over the grandocs (unofficial MA Lighting docs mirror) static index, served via Streamable HTTP.",
	"author": "Sam Carlton",
	"license": "ISC",
	"main": "src/index.ts",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy",
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"@modelcontextprotocol/sdk": "^1.18.0",
		"minisearch": "^7.1.2"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^4.20260601.0",
		"typescript": "^5.7.0",
		"wrangler": "^4.100.0"
	}
}
