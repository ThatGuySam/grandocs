# grandMA2 landmark pages

Routes are `/grandma2/<slug>/`; raw markdown is `/grandma2/<slug>.md`. No version
segment. Cite the page's `sourceUrl` (`help.malighting.com/grandMA2/...`).

## Command line & keywords (for "what's the syntax for…")

grandMA2 command keywords are individual pages slugged **`key_keyword_<name>`**
(~317 pages). Quote the page's syntax block **verbatim** — never invent syntax.

| Topic                         | Slug                                  |
|-------------------------------|---------------------------------------|
| Command syntax & keyword index| `key_command_syntax_and_keywords`     |
| Store                         | `key_keyword_store`                   |
| Assign                        | `key_keyword_assign`                  |
| At                            | `key_keyword_at`                      |
| All                           | `key_keyword_all`                     |
| And                           | `key_keyword_and`                     |
| Align                         | `key_keyword_align`                   |
| Appearance                    | `key_keyword_appearance`              |
| Attribute                     | `key_keyword_attribute`              |
| Backup                        | `key_keyword_backup`                  |
| Blackout                      | `key_keyword_blackout`                |
| Blind                         | `key_keyword_blind`                   |

To resolve any other keyword, slug it as `key_keyword_<lowercased name>` (e.g.
`Goto` → `key_keyword_goto`). If unsure of the exact slug, fetch the index page
`key_command_syntax_and_keywords` (or use the MCP `command_lookup` /
`search_docs` tools) and follow its links.

## Core feature areas

| Topic                  | Slug                          |
|------------------------|-------------------------------|
| Cues & sequences       | `key_cues_and_sequences`      |
| Executors              | `key_exec`                    |
| Advanced executors     | `key_adv_exec`                |
| Presets                | `key_presets`                 |
| Groups                 | `key_groups`                  |
| Macros                 | `key_macro`                   |
| Macro command line     | `key_macro_cli`               |
| Macro examples         | `key_macro_examples`          |
| MAtricks               | `key_matricks`                |
| MAtricks pool          | `key_matricks_pool`           |
| MAtricks wings         | `key_matricks_wings`          |
| onPC details           | `key_onpc_details`            |
| Manual index           | `index` (route `/grandma2/`)  |

These are starting points; the full set is 827 pages. Use `/llms.txt`,
`search_docs`, or the page's own "Subtopics" links to navigate deeper.
